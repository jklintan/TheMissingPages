using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class gameLogic : MonoBehaviour
{
    //Static variables to be shared by the whole game
    static bool lost;
    static bool narniaAccomplished;
    static bool aliceAccomplished;
    static bool harryAccomplished;

    static int collectedPages;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
